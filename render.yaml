services:
  - type: web
    name: puppeteer-api
    env: node
    plan: starter
    buildCommand: |
      npm install
      npx puppeteer browsers install chrome --path=/opt/render/.cache/puppeteer
    startCommand: node index.js
